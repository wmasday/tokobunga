# OS specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project specific
node_modules/
vendor/
.env
.env.local
.env.*.local

# Docker files (if you want to exclude build artifacts, though usually we keep the Dockerfiles)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Database data (volume directories if mapped locally)
mysql_data/

# Specific binary/temporary files
bab1.txt
implementation_plan.md
task.md
